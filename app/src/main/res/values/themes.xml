<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MeMo" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/red</item>
        <item name="colorAccent">@color/red</item>
<!--        <item name="actionBarTheme">@color/red</item>-->
    </style>
    <style name="BottomSheetTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="bottomSheetDialogTheme">@drawable/buttom_drawel</item>
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/red</item>
        <item name="colorAccent">@color/red</item>
    </style>

    <style name="Theme.MeMo" parent="Base.Theme.MeMo" />
</resources>